# 项目范围

用户已明确：本仓库只承担数据飞轮，不代表整套参赛作品。

- 对赛事文档先做条款归属判断，只将数据标注、划分、复核、来源、抽帧复现和数据回流要求用于本模块。
- 用户随后明确纳入 2.2 群体行为时空建模和 2.3 蜂学意义阐释：实现可观测指标、带来源的候选解释和复核回流；不虚构蜂学事件真值或诊断精度。
- 不用 EXE、ONNX、离线 Windows、正式推理输出/退出码、200/600 框上限或六目录材料要求评价飞轮是否完成。除非用户之后明确要求扩展范围。
- 保留源帧编号、已有两点姿态、已选后处理和历史数据版本；格式规范化不得偷偷更换算法或阈值。
- 人工确认、机器候选、隐藏实例和忽略区分别保存；不虚构人工复核、数据划分或下游性能收益。
- 范围的机器可读定义在 configs/flywheel_scope.json，实际标注交付入口为 export-annotations。

## 方法文档范围

- 已有代码实现的联合训练、专家校准、采样增强、主动复核和知识更新保留在方法中；实验尚未完成不是删除方法模块的理由。
- 用户未提供的温度、天气、施药、称重、振动和王台等外部条件，不写成当前方法的输入或推断依据。
- 方法机制与实验结果分别陈述；不将代码实现写成已经取得的实测精度或训练收益。
